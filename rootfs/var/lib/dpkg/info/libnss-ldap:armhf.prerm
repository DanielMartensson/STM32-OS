#!/bin/sh
set -e
# Automatically added by dh_installinit/13.10.1ubuntu1
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/libnss-ldap" ] ; then
	invoke-rc.d libnss-ldap stop || exit 1
fi
# End automatically added section
